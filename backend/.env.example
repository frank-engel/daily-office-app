BIBLE_DB_PATH=data/web.sqlite
HABITS_DB_PATH=data/habits.sqlite
USERS_DB_PATH=data/users.sqlite

# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changeme-replace-with-a-random-secret

# Set to "true" to allow new user registration, "false" to disable
REGISTRATION_ENABLED=false
